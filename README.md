# notas:

este es mi primer programa en node

```
Options:
      --help Show help                 [boolean]
       --version  Show version number   [boolean]
   -b, --base   es la base de la tabla de multiplicar
    [number] [required]

   -l, --listar   muestra la tabla de la consola   [boolean] [required] [default: false]

   -h  este es el numkero hasta donde quiere la tabla
    [number] [default: 10]
```
